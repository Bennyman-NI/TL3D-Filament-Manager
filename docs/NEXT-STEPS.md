# Next steps

1. Run the application once to confirm PySide6 is installed correctly.
2. Capture `docs/spoolman-openapi.json` from the local Spoolman server.
3. Commit the initial project to Git.
4. Add the GitHub remote and push.
5. Build the importer using only fields verified in that schema.
